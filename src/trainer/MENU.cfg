MEMORY {
    LOADADDR:       start = $6000,  size =     2,   file = %O;
    MENU:           start = $6000,  size = $1000,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    MENU:           load = MENU,            type = rw;
}
