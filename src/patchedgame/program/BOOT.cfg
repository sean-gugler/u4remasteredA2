MEMORY {
    LOADADDR:       start = $6000,  size =     2,   file = %O;
    MAIN:           start = $6000,  size = $1800,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    MAIN:           load = MAIN,            type = rw;
}
