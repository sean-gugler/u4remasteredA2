MEMORY {
    LOADADDR:       start = $8000,  size =     2,   file = %O;
    MB_INIT:        start = $8000,  size = $0300,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    MB_INIT:        load = MB_INIT,         type = rw;
}
