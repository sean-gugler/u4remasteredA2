MEMORY {
    LOADADDR:       start = $6400,  size =     2,   file = %O;
    MAIN:           start = $6400,  size = $3340,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    MAIN:           load = MAIN,            type = rw;
}
