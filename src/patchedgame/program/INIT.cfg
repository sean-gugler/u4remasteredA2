MEMORY {
    LOADADDR:       start = $4000,  size =     2,   file = %O;
    MAIN:           start = $4000,  size = $2000,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    MAIN:           load = MAIN,            type = rw;
}
