MEMORY {
    LOADADDR:       start = $8C00,  size =     2,   file = %O;
    DUNGEON_DRAW:   start = $8C00,  size = $0E00,   file = %O;
}
SEGMENTS {
    LOADADDR:       load = LOADADDR,        type = ro;
    DUNGEON_DRAW:   load = DUNGEON_DRAW,    type = rw;
}
